package com.coolcompany.tracker;

public class FriendTracker {

    /**
     * Entry point to the program. This creates an instance the tracker GUI app and runs it.
     * @param args
     */
    public static void main(String[] args) {
        new Tracker("Friend Tracker").run();
    }
}